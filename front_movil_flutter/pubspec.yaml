name: front_movil_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  app_links: ^6.3.3  # ← NUEVO PAQUETE (heredero de uni_links)
  http: ^1.1.0
  provider: ^6.1.1
  shared_preferences: ^2.2.0
  flutter_dotenv: ^5.1.0
  cupertino_icons: ^1.0.8
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/google_logo.png